# Work Items — 1.7.1

Theme: **Brand refresh.** "ESPHome Fleet" → "Fleet for ESPHome", paired with a fresh logo and the brands-repo submission to `home-assistant/brands`. Honest Gold (the originally-themed tier-flip) has slipped to 1.7.2 — the rebrand needed the space, and shipping the brands-repo PR with the new wordmark rather than the old one means the slow-moving brands queue only sees one round-trip, not two. Code identifiers (repo `weirded/distributed-esphome`, add-on slug `esphome_dist_server`, Docker image names `esphome-dist-server`/`esphome-dist-client`, the custom-integration domain `esphome_fleet`, the YAML marker `# distributed-esphome:`) all stay in their existing `distributed_esphome` / `esphome-dist-*` / `esphome_fleet` form per CLAUDE.md so existing installs don't migrate.

Two ESPHome-delegation items (EH.1 / EH.3 / EH.4) carried over from 1.7.0 remain in scope here — small internal cleanups, neither user-visible, fold cleanly into the rebrand cycle.

Scope rule: every workitem below either (a) renames a user-facing surface to "Fleet for ESPHome", (b) refreshes brand artwork end-to-end, (c) is one of the three EH delegation carryovers, or (d) is a numbered bug. Honest Gold's QS / HT / TP / CI / SD workstream lives in `WORKITEMS-1.7.2.md`.

---

## EH — ESPHome-delegation (carryover from 1.7.0)

Audit on 2026-04-22 of `ha-addon/server/` + `ha-addon/client/` looking for places we re-implement functionality ESPHome ships as a library (`esphome.*`) or a CLI (`esphome <subcommand>`). EH.2 landed in 1.6.2-dev.11 along the #84 fix (ESPHome's full validator became the primary resolver). Three remaining items, deferred from 1.7.0 when tags + routing consumed the bandwidth:

- [ ] **EH.1 Use `esphome idedata` for firmware-artifact discovery.** `ha-addon/client/client.py:1225-1275` (`_collect_firmware_variants`) walks `.esphome/build/<name>/.pioenvs/<name>/firmware.{factory,}.bin` via hardcoded path templates. Stable for ESP32/ESP8266 today; breaks the moment ESPHome ships a target platform with a different PlatformIO layout (RP2040, nRF52, Zephyr, the new `host` platform). ESPHome's `esphome idedata <yaml>` (and `esphome.platformio_api.get_idedata()` Python API) emits JSON including `firmware_elf_path`, `firmware_bin_path`, and `extra_flash_images` (list of `{offset, path}` entries for bootloader/partition-table blobs). Fix: after a successful `esphome run`, invoke `<venv>/bin/esphome idedata <yaml>` and parse the JSON as the authoritative artifact manifest; retain today's path walk as a legacy fallback (probe once per job, cache the decision). Bonus: `extra_flash_images` unblocks a future USB-first-flash flow without further reinvention (currently we only archive `firmware.factory.bin` + `firmware.bin`; bootloader + partition table are silently dropped). Test: `tests/test_client_firmware_collection.py` across ESP32 (factory+ota), ESP8266 (ota only), and a synthetic RP2040 fixture; mocked `idedata` JSON drives the parse path and a real compile against `cyd-office-info` proves the wire.

- [ ] **EH.3 Replace magic-string config keys with `esphome.const` imports.** `ha-addon/server/scanner.py` and `ha-addon/client/client.py` reference `"esphome"`, `"name"`, `"wifi"`, `"ethernet"`, `"openthread"`, `"api"`, `"substitutions"`, `"packages"`, `"platform"`, `"framework"`, `"board"`, `"use_address"`, `"manual_ip"`, `"static_ip"`, `"domain"` as string literals scattered across many call sites. ESPHome's own code uses `CONF_ESPHOME`, `CONF_NAME`, `CONF_WIFI`, `CONF_ETHERNET`, `CONF_OPENTHREAD`, `CONF_API`, `CONF_SUBSTITUTIONS`, `CONF_PACKAGES`, `CONF_PLATFORM`, `CONF_FRAMEWORK`, `CONF_BOARD`, `CONF_USE_ADDRESS`, `CONF_MANUAL_IP`, `CONF_STATIC_IP`, `CONF_DOMAIN` from `esphome.const` — any upstream rename becomes an `ImportError` in our layer instead of a silent dict-miss that drops `friendly_name` / `use_address` / etc. for every user. Mechanical sweep; `ruff` + `mypy` + existing test suite catches typos. Scope the change to files where ESPHome is already on `sys.path` (i.e. anywhere touched after `_esphome_ready` fires) — the cold-start fallback paths keep the literal strings so they work before the venv is installed. UD.4's pairing note (use `CONF_BLUETOOTH_PROXY` once the magic-string sweep lands) folds in here — UD.4 itself shipped in 1.7.0-dev.18 via bug #23.

- [ ] **EH.4 Simplify ESPHome version detection.** `ha-addon/server/scanner.py:207-228` (`_get_installed_esphome_version`) shells to `<venv>/bin/esphome version` and string-parses `"Version: X.Y.Z"` on stdout as the primary path; `importlib.metadata.version("esphome")` at lines 232-234 is a fallback. Once the venv's `site-packages` is on `sys.path` (which happens before `_esphome_ready` fires) the two paths return the same answer — the subprocess is redundant and ~50ms of fork+exec on a hot path. Fix: either (a) reorder so `importlib.metadata` is primary and subprocess is the disambiguator when we have reason to believe server-process Python is pointing at a different ESPHome than the venv, or (b) import `esphome.const.__version__` directly (single attr read, no subprocess, no parsing). Keep the current memoization. Lowest priority of the three; bundle with any nearby scanner.py work rather than as a standalone PR. Test: `tests/test_scanner_version.py` with and without the venv activated, asserting the two paths agree and the subprocess is skipped on the warm path.

---

## BR — Brand refresh: "ESPHome Fleet" → "Fleet for ESPHome"

Two workitems. Sequence: BR.1 lands first (strings flip on next deploy, e2e assertions update lockstep), then BR.2 (artwork files in-tree + brands-repo PR opened). User-facing only — code identifiers stay per CLAUDE.md.

- [ ] **BR.1 Rename every user-facing "ESPHome Fleet" → "Fleet for ESPHome".** Scope, in landing order:
  1. **HA add-on metadata** (changes the add-on store name + sidebar entry on next add-on rebuild — slug unchanged, no reinstall needed): `ha-addon/config.yaml` → `name:` + `panel_title:`; `ha-addon/Dockerfile` `io.hass.name=` label; `repository.json` → `name`.
  2. **HA custom integration display name + config-flow copy** (re-renders on next HA Core restart; domain `esphome_fleet` stays so service names `esphome_fleet.compile|cancel|validate` survive): `ha-addon/custom_integration/esphome_fleet/manifest.json:name`; `const.py:DEFAULT_TITLE`; every hit in `strings.json` + `translations/en.json` — `config.flow_title`, `step.user.title`/`description`, `step.discovery_confirm.description`, `step.reauth_confirm.title`, `step.reconfigure_confirm.title`, `issues.config_entry_reauth.title`, `exceptions.no_config_entry.message`, plus error messages mentioning "ESPHome Fleet server / build worker / targets". Re-read once after the sweep — phrasings like *"ESPHome Fleet manages ESPHome devices"* should be rephrased to avoid the awkward "Fleet for ESPHome … ESPHome …" doubling (e.g. *"Fleet for ESPHome manages your ESPHome firmware fleet"*).
  3. **HA device-registry surfaces** (visible under Settings → Devices, refresh on next coordinator tick): `ha-addon/custom_integration/esphome_fleet/device.py` Hub `name="ESPHome Fleet"` + `manufacturer="ESPHome Fleet"`; Worker `manufacturer="ESPHome Fleet Worker"` → "Fleet for ESPHome" / "Fleet for ESPHome Worker".
  4. **Frontend chrome:** `ha-addon/ui/index.html` `<title>`; `ha-addon/ui/src/App.tsx` header wordmark + image `alt=`; `App.tsx` session-expired error toast; `components/VersioningOnboardingModal.tsx` body copy. Update e2e assertions in lockstep so the build doesn't go red mid-PR — `ha-addon/ui/e2e/smoke.spec.ts:15-16`, `e2e/theme-responsive.spec.ts:107`, `e2e-hass-4/cyd-office-info.spec.ts:72-74` each currently assert `getByText('ESPHome Fleet', { exact: true })` and must flip to `'Fleet for ESPHome'`.
  5. **Backend HTTP/log surfaces:** `ha-addon/server/ha_auth.py` Bearer-realm string at line 72 (`_WWW_AUTHENTICATE`) + the auth-error HTML page `<title>` and the visible `<code>` block at lines 102 + 191; `ha-addon/server/main.py` startup + shutdown log lines (~1560/~1751); `ha-addon/server/scanner.py` user-visible error message ("ESPHome Fleet 1.6.2+ requires…") — but **keep the `# distributed-esphome:` YAML marker comment unchanged**, it's a code-identifier-class string the scanner reads, not a user-facing label; `ha-addon/server/integration_installer.py` instruction copy ("Add it via Settings → Devices & Services → ESPHome Fleet."); `ha-addon/server/diagnostics.py` section title; `ha-addon/server/mdns_advertiser.py` `service_name` default — flip to "Fleet for ESPHome" but **verify the HA-side zeroconf discovery in the custom-integration `manifest.json` matches on service type (`_esphomelib._tcp` or whatever we registered), not on the advertised instance name, before merging** — if it matches on instance name, existing installs lose discovery until the integration is re-added; sticky bit, easy to miss.
  6. **Documentation:** `README.md`, `ha-addon/DOCS.md`, `ha-addon/SECURITY.md`, `CONTRIBUTING.md`, `ARCHITECTURE.md`, `ha-addon/custom_integration/esphome_fleet/README.md`, `docs/brands-submission/README.md` — every heading + body mention. **`ha-addon/CHANGELOG.md` is the exception:** prior-release entries describe past releases and must stay accurate. Phrase the 1.7.1 changelog entry as the *current* rebrand and leave older entries untouched, e.g. *"Renamed to **Fleet for ESPHome** (the add-on shipped as **ESPHome Fleet** in 1.5.0–1.7.0; before that **ESPHome Distributed Build Server**)."*
  7. **Bearer-realm backwards-compat watch.** The HTTP 401 response's `WWW-Authenticate: Bearer realm="ESPHome Fleet"` is consumed by external HTTP clients (rare — clients almost universally ignore the realm string). Flip it to `"Fleet for ESPHome"`; if any test or external integration hard-codes the old realm string, fix the consumer rather than freeze the realm.
  8. **Anti-drift gate:** add a one-line rule (call it **PY-12**) to `scripts/check-invariants.sh` that fails CI if `grep -rn "ESPHome Fleet"` returns any hit outside an allowlist of (a) `dev-plans/archive/`, (b) the historical-rebrand line in `ha-addon/CHANGELOG.md`, (c) `dev-plans/WORKITEMS-1.7.1.md` itself (this file). New CLAUDE.md "Enforced Invariants" entry alongside PY-1..PY-11 documenting the rule.
  9. **End-of-PR backwards-compat sweep:** before merge, run the PY-12 grep manually; the only allowed hits are the allowlist entries above. Anything else means the sweep missed a string.

- [ ] **BR.2 New logo + brand artwork (absorbs the previously-listed QS.B1).** Twelve asset files refreshed, then the brands-repo PR opens with the new artwork. Sequence: artwork lands in this repo first, then `home-assistant/brands` PR opens citing the in-repo files as the source of truth.
  1. **Design direction (owner call, not specified by the survey):** the existing house-icon glyph (`ha-addon/ui/src/assets/esphome-logo.svg`, imported at `App.tsx:76`) — does it carry over to "Fleet for ESPHome", or is the new mark a different shape? The wordmark dimensions for the brands repo are fixed at ~3:1 ratio (`logo.png 516×98` / `logo@2x.png 1026×194`), so the wordmark must compose to those proportions. Pick a font family for the wordmark (current treatment is sans-serif at 20px in `App.tsx:779`; the rebrand can keep that or change). These are decisions, not deliverables — capture in a short note when the assets land.
  2. **In-add-on icons** (HA add-on store + sidebar): `ha-addon/icon.png` (128×128) + `ha-addon/icon.svg` (vector source, regenerated from the new design) + `ha-addon/logo.png` (192×192). Bumping `ha-addon/icon.png` is what flips the icon in the HA Supervisor add-on store on next rebuild — sanity-check on hass-4 after deploy.
  3. **HA custom-integration icons** (Settings → Devices & Services card, until brands CDN takes over): `ha-addon/custom_integration/esphome_fleet/icon.png` (128×128) + `logo.png` (192×192). Update these even though HA fetches from the brands CDN once that PR merges — out-of-tree consumers and non-HA contexts still read these.
  4. **Frontend assets:** `ha-addon/ui/public/favicon.svg` + `ha-addon/ui/src/assets/esphome-logo.svg` (the house-glyph imported by `App.tsx:76` next to the wordmark) + `ha-addon/ui/src/assets/hero.png` — verify whether `hero.png` is still referenced; the survey couldn't find a consumer. If unreferenced, delete it rather than refresh. The built copies under `ha-addon/server/static/` regenerate from `npm run build` — do not hand-edit.
  5. **HA brands submission, sized exactly per the `home-assistant/brands` README:** `docs/brands-submission/custom_integrations/esphome_fleet/{icon.png 256×256, icon@2x.png 512×512, logo.png 516×98, logo@2x.png 1026×194}`. The directory keeps the slug `esphome_fleet/` because the brands repo keys off slug, not display name.
  6. **Marketing artwork:** `docs/dist-esphome-logo.png` (1024×1024, embedded in `README.md`). Regenerate or replace.
  7. **Brands-repo PR (formerly QS.B1, lifted into 1.7.1 alongside BR):** open the PR to `home-assistant/brands` per that repo's README — `custom_integrations/esphome_fleet/{icon,icon@2x,logo,logo@2x}.png` matching the in-tree `docs/brands-submission/` files byte-for-byte. Once merged, flip `ha-addon/custom_integration/esphome_fleet/quality_scale.yaml:brands` to `done`. Until merged, leave it `todo` with the open PR URL in the comment so the gate is visible.
  8. **Verification on hass-4 after deploy:** confirm the new icon shows in the HA Supervisor add-on store, the new favicon shows in the browser tab, the integration card on Settings → Devices & Services renders the new wordmark, and the device manufacturer column under Settings → Devices reads "Fleet for ESPHome" / "Fleet for ESPHome Worker".

---

## Open Bugs & Tweaks

### Carried forward from 1.7.0

*(Any post-tag regression against `v1.7.0` lands here as a numbered bug once 1.7.0 ships.)*

### New in 1.7.1

- [x] **#231** *(1.7.1-dev.2)* — "Authentication Expired" repair sent users to the wrong place. The reauth flow, the initial config flow, the `token_required` error, the `config_entry_reauth` fix-flow description, and the underlying `ConfigEntryAuthFailed` exception message in `coordinator.py:103-106` all directed users to "Settings → Add-ons → ESPHome Fleet → Configuration" — the HA Supervisor's add-on Configuration tab. The Server token actually lives in the add-on's own UI at **Settings → Authentication → Server token** (`SettingsDrawer.tsx:193-199`). Reported by BradleyFord in #108. Fix: rewrote the four affected entries in `strings.json` and `translations/en.json`, plus the exception message, to reference the add-on's Settings drawer path verbatim ("Settings → Authentication → Server token") so the in-product copy matches the visible UI labels.
