[pytest]
testpaths = tests
asyncio_mode = auto
# pytest-xdist: distribute tests across all available CPU cores.
# Falls back to sequential if xdist is not installed.
addopts = -n auto
markers =
    integration: tests that run a real ESPHome compile (slow, requires esphome on PATH)
